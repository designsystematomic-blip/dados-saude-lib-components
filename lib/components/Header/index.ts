console.log("Index file for Header component");
import Action from "./Action";
import Logo from "./Logo";
import Root from "./Root";
import Wrapper from "./Wrapper";

const Header = {
  Action,
  Logo,
  Root,
  Wrapper,
};

export default Header;
