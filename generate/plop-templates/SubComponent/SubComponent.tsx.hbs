import type { {{pascalCase part}}Props } from './{{pascalCase part}}.types';
import styles from './{{pascalCase part}}.module.css';

const {{pascalCase part}} = ({ props }:{{pascalCase part}}Props) => {
  return <div className="{{dashCase part}}">{children}</div>;
};

export default {{pascalCase part}};
